import { Command } from '@oclif/core';
import { AndroidAppIcon, ANDROID_CPU, CliConfig, License, NpmAsset, TemplateMeta } from '../definition';
export default class Create extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        force: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
        config: import("@oclif/core/lib/interfaces").OptionFlag<string | undefined, import("@oclif/core/lib/interfaces/parser").CustomOptions>;
    };
    static args: {
        projectName: import("@oclif/core/lib/interfaces/parser").Arg<string, Record<string, unknown>>;
    };
    _existProject(destDir: string, force?: boolean): Promise<void>;
    _promptValues(cliConfig: CliConfig): Promise<{
        license: License;
        cpus: ANDROID_CPU[];
        iosAppName: string;
        androidAppName: string;
        libraries: NpmAsset[];
        template: TemplateMeta;
    }>;
    _guardIcons(value: any): {
        ios?: string;
        android?: AndroidAppIcon[];
    };
    _guardValues(value: any, config: CliConfig): {
        cpus: ANDROID_CPU[];
        androidAppName: string;
        iosAppName: string;
        libraries: NpmAsset[];
        template: {
            name: string;
            provider: 'gitea' | 'npm';
        };
    };
    run(): Promise<void>;
}
