import { Command } from '@oclif/core';
export default class License extends Command {
    static description: string;
    static examples: string[];
    static flags: {};
    static args: {
        appId: import("@oclif/core/lib/interfaces/parser").Arg<string | undefined, Record<string, unknown>>;
    };
    private getLicenseByAppId;
    private choiceLicense;
    run(): Promise<void>;
}
