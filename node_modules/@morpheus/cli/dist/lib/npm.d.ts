import { NpmPackage, NpmSearchPackage } from '../definition';
interface NpmCliConfig {
    package: {
        version: string;
    };
    npmRegistryUrl: string;
    disabledTLSCerts?: boolean;
}
export declare function searchNpmPackagesByScope(scope: string, config: NpmCliConfig): Promise<NpmSearchPackage>;
export declare function searchNpmPackage(packageName: string, config: NpmCliConfig): Promise<NpmPackage>;
export {};
