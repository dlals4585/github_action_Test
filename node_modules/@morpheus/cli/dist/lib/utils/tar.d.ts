import { SuperAgentRequest } from '../../definition';
export declare function downloadAndUnzip(req: SuperAgentRequest, dir: string, { progress }: {
    progress?: (loaded: number, total: number) => void;
}): Promise<void>;
