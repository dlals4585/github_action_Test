import { SuperAgentRequest, GiteaRelease, GiteaRepository, TemplateConfigByGitea } from '../definition';
import { HttpMethod } from './utils/superagent';
interface GiteaCliConfig {
    package: {
        version: string;
    };
    template: Omit<TemplateConfigByGitea, 'mode'>;
    disabledTLSCerts?: boolean;
}
export declare function createGiteaRequest(method: HttpMethod, url: string, config: GiteaCliConfig): Promise<{
    req: SuperAgentRequest;
}>;
export declare function getTemplates(config: GiteaCliConfig): Promise<GiteaRepository[]>;
export declare function getLatestRelease(templateName: string, config: GiteaCliConfig): Promise<GiteaRelease>;
export {};
